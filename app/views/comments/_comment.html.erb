<div class="panel">
  <%= comment.comment %><br>
  by <%= comment.author %> <br>
  <%= comment.created_at %>

  <% if current_user && (comment.user_id === current_user.id || current_user.admin? == true) %>

  
    <% end %>
</div>
